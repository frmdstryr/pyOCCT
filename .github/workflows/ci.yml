on:
  push:
    branches:
    - '*'
  #pull_request:
  #  branches:
  #  - '*'
jobs:
  build:
    name: Build (${{ matrix.python-version }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: ['ubuntu-latest', 'macos-latest', 'windows-latest']
        python-version: ['3.6']
    steps:
      - uses: goanpeca/setup-miniconda@v1
        with:
          auto-update-conda: true
          python-version: ${{ matrix.python-version }}
      - name: Install OpenGL headers
        if: matrix.os == 'ubuntu-latest'
        run: |
            sudo apt-get update
            sudo apt-get -q -y install libglu1-mesa-dev libgl1-mesa-dev libxi-dev  libglfw3-dev libgles2-mesa-dev
      - name: Configure conda
        shell: bash -l {0}
        run: |
            conda info
            conda config --add channels conda-forge
      - name: Create env
        shell: bash -l {0}
        run: |
            conda create --yes --quiet --name pyOCCT python=${{ matrix.python-version }} conda-build
      - name: Build
        shell: bash -l {0}
        run: |
            source activate pyOCCT
            conda build ci/conda
